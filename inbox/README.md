# Inbox

这里保存尚未完成 Prompt Pair 分析的真实 Prompt。

建议文件名：`YYYY-MM-DD-short-topic.md`。

最低内容：

- 原始 Prompt
- 目标模型 / 工具
- 真实任务背景（只写会改变判断的部分）
- 发送前最不确定的一点
- 如果已经执行：结果、返修轮次、验证证据

完成分析后移动到 `prompt-pairs/<ability>/`。只有跨多个案例重复出现且被真实任务验证的规律，才能进一步晋升到 `principles/` 或 `patterns/`。
